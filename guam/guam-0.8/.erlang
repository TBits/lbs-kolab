        application:start(compiler).
        application:start(syntax_tools).
        application:start(goldrush).
    application:start(lager).

    application:start(crypto).
application:start(kolab_guam).


