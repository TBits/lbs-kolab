#!/usr/bin/make -f
# Uncomment this to turn on verbose mode.
export DH_VERBOSE=1

%:
	dh $@

override_dh_install:
	mkdir -p $(CURDIR)/debian/tmp/
	cp -a dblog $(CURDIR)/debian/tmp/dblog
	dh_install 

